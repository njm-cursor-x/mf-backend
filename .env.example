# Plaintext key used only to seed the hashed api_keys table on boot.
# Mint a real one with: python -m app.keys create --name grok-voice
MF_API_KEY=mf_live_replace_me
MF_DB_PATH=data/showtimes.db
MF_TZ=America/New_York
